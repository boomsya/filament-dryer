var iot;
!function (a) {
	"use strict";
	var b;
	b = {
		switchSingle: function (b, c) {
			a('[data-unit="' + b + '"]').toggleClass("active");
			a("#" + b).closest("label").toggleClass("checked", c);
		},
		switchEnable: function (b, enable) {
			if (enable) {
				a('#' + b).removeAttr("disabled");
				a('#' + b).parent().removeClass("disabled");
			} else {
				a('#' + b).attr("disabled", "true");
				a('#' + b).parent().addClass("disabled");
			}
		},
		positionFab: function () {
			var b = a("#main"), c = !!b.get(0) && b.get(0).scrollHeight > b.innerHeight();
			c && a("#info-toggler").css("right", "40px")
		}
	}, window.iot = b
}(this.jQuery);